# Core
numpy>=1.20.0
pandas>=1.3.0
python-dotenv>=1.0.0

# ── External API clients ──────────────────────────────────────────────────────
deepgram-sdk>=3.0.0          # STT: Deepgram Nova-3
google-genai>=1.0.0          # LLM: Gemini 2.5 Flash (native audio)
hume>=0.7.0                  # Emotion: Hume Expression Measurement

# ── Audio processing ──────────────────────────────────────────────────────────
librosa>=0.9.0
pydub>=0.25.0
ffmpeg-python>=0.2.0         # Requires ffmpeg installed separately

# ── Text processing ───────────────────────────────────────────────────────────
nltk>=3.6.0
pronouncing>=0.2.0

# ── Vector store & embeddings ─────────────────────────────────────────────────
chromadb>=0.4.0
sentence-transformers>=2.2.0

# ── Web framework ─────────────────────────────────────────────────────────────
fastapi>=0.110.0
uvicorn[standard]>=0.29.0
python-multipart>=0.0.9

# ── Dev / notebook ────────────────────────────────────────────────────────────
ipykernel>=6.0.0

# ── Removed ───────────────────────────────────────────────────────────────────
# ollama          — replaced by Gemini via google-generativeai
# openai-whisper  — replaced by Deepgram via deepgram-sdk
# bitsandbytes    — quantization only needed for local model training
# sounddevice     — only used by main.py live-recording (deleted)
# soundfile       — only used by main.py live-recording (deleted)
# torch           — only used by create_finetune_data.py (deleted)
# transformers    — only used by create_finetune_data.py (deleted)
# datasets        — only used by create_finetune_data.py (deleted)
# peft            — only used by create_finetune_data.py (deleted)
# accelerate      — only used by create_finetune_data.py (deleted)
# pytube          — only used by data_collector.py (orphaned)
# youtube-transcript-api — only used by data_collector.py (orphaned)
# yt-dlp          — only used by data_collector.py (orphaned)
